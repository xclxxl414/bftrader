加权指数计算方法：
tick级别的，跟随主力合约推送
主力合约tick到，推主力合约tick
查非主力合约最新的持仓-成交量和lastprice，把价格给加权平均掉，持仓和成交量求和。

还有一种，就是每个合约的tick来了，都推一下指数。
跟着主力走，同步计算，很好。

实现一个方案：订阅主力，自动订阅全部，计算指数，可以选择接收指数。

以后再说，通过多个gateway组装就行了。单独做个gateway，可以让策略订阅。cta就可以连接两个gateway了。
